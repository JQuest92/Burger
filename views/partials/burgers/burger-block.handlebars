<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}DEVOURING IT NOW NOMNOMNOM!{{else}}COOKING IT UP, NOW!{{/if}}
    </button>
</li>